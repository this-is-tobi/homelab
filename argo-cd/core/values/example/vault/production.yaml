vault:
  server:
    ha:
      apiAddr: "vault.core.example.com"
    ingress:
      hosts:
      - host: "vault.core.example.com"
        paths:
        - "/"
      tls:
      - hosts:
        - "vault.core.example.com"
        secretName: "vault.core.example.com-tls"
