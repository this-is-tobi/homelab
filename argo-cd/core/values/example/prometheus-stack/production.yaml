kube-prometheus-stack:
  prometheus:
    prometheusSpec:
      storageSpec:
        volumeClaimTemplate:
          spec:
            resources:
              requests:
                storage: 100Gi
  grafana:
    admin:
      existingSecret: "prometheus-stack-grafana-admin"
      userKey: "username"
      passwordKey: "password"
    ingress:
      hosts:
      - "monitoring.core.example.com"
      tls:
      - hosts:
        - "monitoring.core.example.com"
        secretName: "monitoring.core.example.com"
    grafana.ini:
      server:
        root_url: "https://monitoring.core.example.com"
      auth.generic_oauth:
        client_id: "grafana"
        client_secret: ""
        auth_url: "https://keycloak.core.example.com/realms/homelab/protocol/openid-connect/auth"
        token_url: "https://keycloak.core.example.com/realms/homelab/protocol/openid-connect/token"
        api_url: "https://keycloak.core.example.com/realms/homelab/protocol/openid-connect/userinfo"
