apiVersion: v2
type: application
name: traefik
version: 41.0.1
appVersion: "v3.7.5"
description: >-
  Traefik v3 edge router — standing replacement for APISIX as the cluster
  ingress + Gateway API implementation (see docs/08-ingress-controller-analysis.md
  and docs/09-traefik-migration.md). Stateless: all config lives in the
  Kubernetes API, no external store, no admin API.
dependencies:
- name: traefik
  alias: traefik
  version: 41.0.1
  repository: https://traefik.github.io/charts
