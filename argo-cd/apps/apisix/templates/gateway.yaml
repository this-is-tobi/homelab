{{- if .Values.gateway.enabled -}}
---
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: apisix
spec:
  controllerName: apisix.apache.org/apisix-ingress-controller
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ .Values.gateway.name | default "homelab-gateway" }}
  namespace: {{ .Values.gateway.namespace | default "kube-system" }}
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.gateway.dnsClusterIssuer | default "letsencrypt-dns-prod" }}
spec:
  gatewayClassName: apisix
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All
    - name: https-wildcard
      protocol: HTTPS
      port: 443
      hostname: "*.{{ .Values.gateway.domain }}"
      tls:
        mode: Terminate
        certificateRefs:
          - name: {{ .Values.gateway.wildcardTlsSecret | default "gateway-wildcard-tls" }}
      allowedRoutes:
        namespaces:
          from: All
    - name: https-apex
      protocol: HTTPS
      port: 443
      hostname: "{{ .Values.gateway.domain }}"
      tls:
        mode: Terminate
        certificateRefs:
          - name: {{ .Values.gateway.apexTlsSecret | default "gateway-wildcard-tls" }}
      allowedRoutes:
        namespaces:
          from: All
{{- range .Values.gateway.extraListeners }}
    - {{ toYaml . | nindent 6 }}
{{- end }}
{{- if .Values.gateway.tlsPassthroughListeners }}
{{- range .Values.gateway.tlsPassthroughListeners }}
    - name: {{ .name }}
      protocol: TLS
      port: 443
      hostname: {{ .hostname | quote }}
      tls:
        mode: Passthrough
      allowedRoutes:
        namespaces:
          from: All
{{- end }}
{{- end }}
---
# Wildcard certificate for *.{{ .Values.gateway.domain }} and {{ .Values.gateway.domain }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gateway-wildcard-tls
  namespace: {{ .Values.gateway.namespace | default "kube-system" }}
spec:
  secretName: {{ .Values.gateway.wildcardTlsSecret | default "gateway-wildcard-tls" }}
  issuerRef:
    name: {{ .Values.gateway.dnsClusterIssuer | default "letsencrypt-dns-prod" }}
    kind: ClusterIssuer
  dnsNames:
    - "*.{{ .Values.gateway.domain }}"
    - "{{ .Values.gateway.domain }}"
---
# HTTP → HTTPS redirect
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-to-https-redirect
  namespace: {{ .Values.gateway.namespace | default "kube-system" }}
spec:
  parentRefs:
    - name: {{ .Values.gateway.name | default "homelab-gateway" }}
      namespace: {{ .Values.gateway.namespace | default "kube-system" }}
      sectionName: http
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
{{- end }}
