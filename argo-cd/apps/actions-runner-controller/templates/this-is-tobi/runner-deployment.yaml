apiVersion: actions.summerwind.dev/v1alpha1
kind: RunnerDeployment
metadata:
  name: this-is-tobi-runner
spec:
  template:
    spec:
      repository: this-is-tobi/tools
      githubAPICredentialsFrom:
        secretRef:
          name: this-is-tobi-secret
