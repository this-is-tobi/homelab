gateway:
  enabled: true
  gatewayName: homelab-gateway-traefik
  gatewayNamespace: kube-system
  routes:
  - name: longhorn
    listener: https-wildcard-int
    hostnames:
    - longhorn.int.ohmlab.fr
    rules:
    - backendService: longhorn-oauth2-proxy
      backendPort: 80
      # LAN-only (post-migration-hardening.md L6), oauth2-proxy stays on
      # as a second factor — this narrows WHERE the login page is even
      # reachable from, it doesn't replace auth.
      filters:
      - type: ExtensionRef
        extensionRef:
          group: traefik.io
          kind: Middleware
          name: lan-only

vso:
  vaultStaticSecrets:
    longhornSso:
      mount: homelab
      path: platforms/production/core/longhorn
      type: kv-v2
      vaultAuthRef: longhorn-system-vso-default
      refreshAfter: 1h
      hmacSecretData: true
      destination:
        name: longhorn-oauth2-proxy-creds
        create: true
        type: Opaque
        transformation:
          excludes:
          - ".*"
          templates:
            client-id:
              text: '{{ get .Secrets "keycloak" | dig "clientId" "" }}'
            client-secret:
              text: '{{ get .Secrets "keycloak" | dig "clientSecret" "" }}'
            cookie-secret:
              text: '{{ get .Secrets "keycloak" | dig "cookieSecret" "" }}'
  extraObjects:
  - apiVersion: v1
    kind: ServiceAccount
    metadata:
      name: vso
      namespace: longhorn-system

sso:
  config:
    configFile: |-
      provider = "keycloak-oidc"
      provider_display_name = "Keycloak"
      email_domains = [ "*" ]
      upstreams = [ "http://longhorn-frontend.longhorn-system.svc.cluster.local:80/" ]
      redirect_url = "https://longhorn.int.ohmlab.fr/oauth2/callback"
      oidc_issuer_url = "https://sso.core.ohmlab.fr/realms/homelab"
      reverse_proxy = true
      cookie_secure = true
      cookie_domains = [ "longhorn.int.ohmlab.fr" ]

longhorn:
  ingress:
    enabled: false

# Zero-trust NetworkPolicy. See .local/post-migration-hardening.md §2 for
# the 5 empirically-tested facts this design depends on. iSCSI/NFS rules
# below use the same pod-CIDR ipBlock as the webhook fact by structural
# analogy (remote node's host-namespace process -> pod), NOT independently
# packet-tested for iSCSI/NFS specifically — recommend a live volume
# attach/detach test during this namespace's rollout step.
networkPolicy:
  enabled: true
  egressDeny: true
  prometheusScrapePorts:
  - { protocol: TCP, port: 9500 }   # longhorn-manager, SM port "manager"
  extraIngress:
  - name: webhook
    podSelector:
      matchLabels: { app: longhorn-manager }
    rules:
    - from:
      - ipBlock: { cidr: 10.45.0.0/16 }
      ports:
      - { protocol: TCP, port: 9502 }
  - name: iscsi
    podSelector:
      matchLabels: { longhorn.io/component: instance-manager }
    rules:
    - from:
      - ipBlock: { cidr: 10.45.0.0/16 }
      ports:
      - { protocol: TCP, port: 3260 }
  - name: nfs
    podSelector:
      matchLabels: { longhorn.io/component: share-manager }
    rules:
    - from:
      - ipBlock: { cidr: 10.45.0.0/16 }
      ports:
      - { protocol: TCP, port: 2049 }
  - name: sso
    podSelector:
      matchLabels: { app.kubernetes.io/name: sso, app.kubernetes.io/instance: longhorn-system }
    rules:
    - from:
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: traefik
      ports:
      - { protocol: TCP, port: 4180 }
  extraEgress:
  - name: apiserver
    rules:
    - to:
      - ipBlock: { cidr: 192.168.1.0/24 }
      ports:
      - { protocol: TCP, port: 6443 }
  - name: sso-hairpin
    podSelector:
      matchLabels: { app.kubernetes.io/name: sso, app.kubernetes.io/instance: longhorn-system }
    rules:
    - to:
      - ipBlock: { cidr: 82.66.77.140/32 }
      ports:
      - { protocol: TCP, port: 443 }
