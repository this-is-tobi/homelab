argo-workflows:
  server:
    ingress:
      hosts:
      - "argo-worflows.staging.example.com"
      tls:
      - secretName: "argo-worflows.staging.example.com-tls"
        hosts:
        - "argo-worflows.staging.example.com"
    sso:
      issuer: "https://keycloak.staging.example.com/realms/homelab"
      redirectUrl: https://argo-worflows.staging.example.com/oauth2/callback
  crds:
    install: true
    keep: true

vso:
  vaultStaticSecrets:
    argoWorkflowsSso:
      mount: "example"
      path: "platforms/staging/argo-workflows"
