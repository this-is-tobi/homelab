# Init
- name: Init
  ansible.builtin.include_role:
    name: services/utils/initialize

# Install
- name: Sync application
  include_tasks: roles/services/utils/synchronize/tasks/refresh.yml
  vars:
    argocd_app: trivy-operator
