- name: Deploy dashy application
  kubernetes.core.k8s:
    state: present
    template: "{{ item }}"
  with_items:
    - namespace.yml.j2
    - configmap.yml.j2
    - service.yml.j2
    - deployment.yml.j2
    - ingress.yml.j2
