apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: le-fabnum-staging
spec:
  acme:
    email: {{ certManager.email }}
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: le-fabnum-staging-key
    solvers:
      - http01:
          ingress:
            serviceType: NodePort # ClusterIP
            ingressClassName: {{ ingressClassName }}

apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: le-fabnum-prod
spec:
  acme:
    email: {{ certManager.email }}
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: le-fabnum-prod-key
    solvers:
      - http01:
          ingress:
            serviceType: NodePort # ClusterIP
            ingressClassName: {{ ingressClassName }}
