---
- name: Add cloud native postgres helm repo
  kubernetes.core.helm_repository:
    name: "{{ cloudNativePostgres.helm.repoName }}"
    repo_url: "{{ cloudNativePostgres.helm.repoUrl }}"
    state: present

- name: Deploy cloud native postgres helm chart
  kubernetes.core.helm:
    name: cnpg
    update_repo_cache: true
    chart_ref: "{{ cloudNativePostgres.helm.chartRef }}"
    chart_version: "{{ cloudNativePostgres.helm.chartVersion }}"
    create_namespace: true
    namespace: "{{ cloudNativePostgres.namespace }}"
    wait: true
    timeout: 10m0s
    state: present
