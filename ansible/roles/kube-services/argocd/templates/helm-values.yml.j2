---
global:
  storageClass: longhorn

applicationSet:
  enabled: true

config:
  secret:
    argocdServerAdminPassword: {{ argocd_password }}

server:
  nodeSelector:
    node-type: worker
  url: https://{{ argocd_domain }}
  insecure: true
  ingress:
    enabled: false

