---
global:
  storageClass: longhorn

applicationSet:
  enabled: true

config:
  secret:
    argocdServerAdminPassword: {{ argocd.password }}

server:
  nodeSelector:
    node-type: worker
  url: https://{{ argocd.domain }}
  insecure: true
  ingress:
    enabled: false

