- name: Add helm repo
  kubernetes.core.helm_repository:
    name: "{{ grafana.helm.repoName }}"
    repo_url: "{{ grafana.helm.repoUrl }}"
    state: present

- name: Deploy helm chart
  kubernetes.core.helm:
    name: "{{ grafana.helm.chartName }}"
    update_repo_cache: true
    chart_ref: "{{ grafana.helm.repoName }}/{{ grafana.helm.chartName }}"
    chart_version: "{{ grafana.helm.chartVersion }}"
    create_namespace: true
    namespace: "{{ grafana.namespace }}"
    wait: true
    timeout: 10m0s
    state: present
    values: "{{ lookup('template', 'helm-values.yml.j2') | from_yaml }}"
