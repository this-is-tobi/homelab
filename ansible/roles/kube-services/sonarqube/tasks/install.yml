- name: Add helm repo
  kubernetes.core.helm_repository:
    name: "{{ sonarqube.helm.repoName }}"
    repo_url: "{{ sonarqube.helm.repoUrl }}"
    state: present

- name: Deploy helm chart
  kubernetes.core.helm:
    name: "{{ sonarqube.helm.chartName }}"
    update_repo_cache: true
    chart_ref: "{{ sonarqube.helm.repoName }}/{{ sonarqube.helm.chartName }}"
    chart_version: "{{ sonarqube.helm.chartVersion }}"
    create_namespace: true
    namespace: "{{ sonarqube.namespace }}"
    wait: true
    timeout: 10m0s
    state: present
    values: "{{ lookup('template', 'helm-values.yml.j2') | from_yaml }}"
