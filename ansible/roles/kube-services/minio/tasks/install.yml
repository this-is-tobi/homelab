- name: Add helm repo
  kubernetes.core.helm_repository:
    name: "{{ minio.helm.repoName }}"
    repo_url: "{{ minio.helm.repoUrl }}"
    state: present

- name: Deploy helm chart
  kubernetes.core.helm:
    name: "{{ minio.helm.chartName }}"
    update_repo_cache: true
    chart_ref: "{{ minio.helm.repoName }}/{{ minio.helm.chartName }}"
    chart_version: "{{ minio.helm.chartVersion }}"
    create_namespace: true
    namespace: "{{ minio.namespace }}"
    wait: true
    timeout: 10m0s
    state: present
    values: "{{ lookup('template', 'helm-values.yml.j2') | from_yaml }}"
