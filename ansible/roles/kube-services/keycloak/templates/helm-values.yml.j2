---
auth:
  adminUser: {{ services.keycloak.username }}
  adminPassword: {{ services.keycloak.password }}

nodeSelector:
  node-type: worker

ingress:
  enabled: true
  ingressClassName: traefik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hostname: {{ services.keycloak.domain }}
  path: /
  tls: true
  secrets: []

production: true

tls:
  enabled: true
  autoGenerated: true

global:
  storageClass: longhorn
