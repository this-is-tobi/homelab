---
- name: Create keycloak client
  community.general.keycloak_client:
    auth_client_id: admin-cli
    auth_keycloak_url: https://{{ services.keycloak.domain }}
    auth_realm: master
    auth_username: "{{ services.keycloak.username }}"
    auth_password: "{{ services.keycloak.password }}"
    realm: "{{ services.keycloak.realm }}"
    client_id: "{{ services.keycloak.clientIds.harbor }}"
    name: "{{ services.keycloak.clientIds.harbor }}"
    enabled: true
    frontchannel_logout: true
    redirect_uris:
      - https://{{ services.harbor.domain }}/c/oidc/callback
    web_origins:
      - https://{{ services.harbor.domain }}
    standard_flow_enabled: true
    implicit_flow_enabled: false
    direct_access_grants_enabled: false
    clientAuthenticatorType: client-secret
    public_client: false
    protocol: openid-connect
    default_client_scopes:
      - generic
      - groups
    state: present
