---
galaxy_info:
  author: this-is-tobi
  role_name: wireguard
  namespace: gateway
  license: MIT
  description: Deploy WireGuard VPN (wg-easy) via Docker Compose.
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm]
  galaxy_tags: [wireguard, vpn, gateway]
dependencies:
  - role: common/docker
