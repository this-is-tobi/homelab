# Pi-hole v6 image: the v5 WEBPASSWORD variable is ignored — only the
# FTLCONF_* form is read. Without it the container boots with a RANDOM admin
# password (printed in its log), silently defeating the password handling.
FTLCONF_webserver_api_password={{ gateway_pihole_password }}
