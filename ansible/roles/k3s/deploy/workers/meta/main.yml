---
galaxy_info:
  author: this-is-tobi
  role_name: workers
  namespace: k3s
  license: MIT
  description: Deploy K3s agent (worker) nodes.
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm]
  galaxy_tags: [k3s, kubernetes, worker, agent]
dependencies:
  - role: k3s/download
